pub get_messages;

get_messages :: Uuid -> Int -> String;
get_messages user_id page = messages
	|> filter (fn x = x.id == user_id)
	|> skip PAGE_SIZE * page
	|> take PAGE_SIZE
	|> map (fn x = (x.id, x.content, x.timestamp));
